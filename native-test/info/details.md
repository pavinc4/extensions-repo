# Native Test

Dev tool for testing that the platform can launch a compiled executable via `run.ps1`.

## How it works

- Toggle **ON** — launches `app/test.exe` directly
- Toggle **OFF** — kills the process

Place your compiled `test.exe` inside the `app/` folder before enabling. Delete this tool once native exe launching is confirmed working.
