# bin/

Place these files here before the extension works:

- `any-downloader.exe` — built from `../src/` using `npm run tauri build`
- `yt-dlp.exe`         — https://github.com/yt-dlp/yt-dlp/releases/latest
- `ffmpeg.exe`         — https://github.com/BtbN/FFmpeg-Builds/releases

See README.md in the root for full build instructions.
